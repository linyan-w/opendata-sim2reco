% Model schematic for the MINERvA sim->reco surrogate. Compile: bash scripts/render_schematic.sh
% Status colours: green = done, orange = in progress, grey = planned. Update this file as the design evolves.
\documentclass[tikz,border=6pt]{standalone}
\usetikzlibrary{positioning,fit,arrows.meta,calc,backgrounds,shapes.misc}
\usepackage[T1]{fontenc}
\usepackage{helvet}\renewcommand{\familydefault}{\sfdefault}
\definecolor{done}{HTML}{2E8B57}
\definecolor{wip}{HTML}{E69F00}
\definecolor{plan}{HTML}{8C8C8C}
\definecolor{ink}{HTML}{222222}
\definecolor{paper}{HTML}{FFFFFF}
\definecolor{soft}{HTML}{F3F4F6}
\tikzset{
  box/.style={draw=ink, line width=0.6pt, rounded corners=3pt, fill=paper, align=left, inner sep=6pt, font=\small, text=ink},
  status/.style={box, line width=1.4pt},
  stage/.style={font=\footnotesize\bfseries, text=ink, anchor=south west},
  note/.style={font=\scriptsize, text=ink!75, align=left},
  arr/.style={-{Latex[length=2.2mm]}, line width=0.7pt, draw=ink},
  arrd/.style={arr, dashed},
  group/.style={draw=ink!35, rounded corners=6pt, fill=soft, inner sep=8pt},
}
\begin{document}
\begin{tikzpicture}[node distance=7mm and 9mm]

% ---------- column 1: truth side ----------
\node[status, draw=done] (gen) {\textbf{Generator final state} (any generator)\\
  particles $(\mathrm{pdg},\,p_x,p_y,p_z)$ after FSI\\
  vertex $(x,y,z)$, struck nucleus $(Z,A)$};
\node[status, draw=done, below=of gen] (pipe) {\textbf{Input pipeline} \S13.1\\
  drop $\nu$, $n$, pseudo-particles, remnants\\
  hadron KE $\ge 50$\,MeV; $\pi^0$ kept as one token\\
  pdg $\to$ 11 species classes};
\node[status, draw=done, below=of pipe] (tok) {\textbf{Model input} \S13.2\\
  particle set $\{(\mathrm{class}_i,\;p_{x,i},p_{y,i},p_{z,i})\}_{i\le n}$\\
  context $c=(x,y,z,Z,A)$};

% ---------- column 2: network ----------
\node[status, draw=wip, right=26mm of pipe.east, anchor=west] (enc)
  {\textbf{Set encoder} (M2, training)\\
  class embedding $\oplus$ scaled $p$ per token\\
  $+$ global token from $c$\\
  transformer, no positional encoding\\
  $\Rightarrow$ event embedding $z$, token states $h_i$};

\node[status, draw=wip, below=9mm of enc.south west, anchor=north west, xshift=-4mm] (t0)
  {\textbf{Tier 0 heads}\\
  $p(\mathrm{reco\,exists}\mid z)$\\
  $p(\mathrm{MINOS\,ok}\mid z)$\\
  $p(\mathrm{charge}<0\mid z)$};
\node[status, draw=wip, below=6mm of t0.south west, anchor=north west] (t1)
  {\textbf{Tier 1 generative} (M2, training)\\
  conditional flow matching on 9-d\\
  $\mu\,(p_x,p_y,p_z)$, reco vertex $(x,y,z)$,\\
  3 calorimetric energies (+2 derived)\\
  conditioned on $z$ and Tier 0 flags};
\node[status, draw=plan, right=6mm of t1] (t2)
  {\textbf{Tier 2 generative} (M4)\\
  flow matching on masked $N\times d$ set\\
  per prong: $\pi$-hyp.\ $p$, proton fit flag,\\
  proton $P$, score, exiting flag\\
  equivariant net, cross-attends $h_i$};
\node[status, draw=wip, above=6mm of t2.north, anchor=south] (card)
  {\textbf{Cardinality}\\
  $p(N_\mathrm{prongs}\mid z)$\\
  categorical $0..8$};

% ---------- column 3: output ----------
\node[status, draw=done, right=20mm of t2] (dec)
  {\textbf{Decoder} \S13.4\\
  beam-frame angles, on-shell $E$\\
  primary / secondary proton rule\\
  fill conventions ($-1$, $-9$, $-9999$)};
\node[status, draw=done, below=of dec] (nt)
  {\textbf{Pruned MasterAnaDev ntuple}\\
  same branch names, types, \texttt{\_sz}\\
  as the open-data tuple};

% ---------- training data (bottom) ----------
\node[status, draw=done, below=26mm of t1.south west, anchor=north west, xshift=-2mm] (data)
  {\textbf{Training pairs} (MINERvA ME FHC MC)\\
  \texttt{Truth} tree: all events $\to$ efficiency denominator\\
  \texttt{MasterAnaDev} tree: reco $+$ truth copy\\
  streamed from xrootd, slimmed to Parquet ($\le 9.5$\,GB)};
\node[status, draw=done, right=6mm of data] (base)
  {\textbf{M1 baselines} (done)\\
  48 summary features $\to$ HistGBDT (Tier 0, $N$)\\
  MDN $K{=}8$: $\mu$ response, $\log E_\mathrm{recoil}$\\
  reference numbers in \texttt{reports/performance}};

% ---------- arrows ----------
\draw[arr] (gen) -- (pipe);
\draw[arr] (pipe) -- (tok);
\draw[arr] (tok.east) -- ++(8mm,0) |- (enc.west);
\draw[arr] (enc.south) -- ++(0,-4.5mm) -| (t0.north);
\draw[arr] (enc.south) -- ++(0,-4.5mm) -| (card.north);
\draw[arr] (t0.south) -- (t0.south |- t1.north);
\draw[arr] (card.south) -- (t2.north);
\draw[arrd] (t0.east) -- (t0.east -| card.west);
\draw[arrd] (t1.east) -- (t2.west);
\draw[arr] (t2.east) -- (dec.west);
\draw[arr] ($(t1.north east)+(-10mm,0)$) -- ++(0,3mm) -| ($(dec.west)+(-7mm,0)$) -- (dec.west);
\draw[arr] (dec) -- (nt);
\draw[arrd] (data.north) -- ++(0,6mm) -| ($(t1.south)+(-8mm,0)$);
\draw[arrd] (base.north) -- ++(0,6mm) -| ($(t2.south)+(8mm,0)$);

% ---------- stage labels ----------
\node[stage, above=2mm of gen.north west] {TRUTH SIDE};
\node[stage, above=2mm of enc.north west] {SURROGATE $p(R \mid X, c)$};
\node[stage, above=9mm of dec.north west] {RECO SIDE};

% ---------- legend ----------
\node[note, below=4mm of data.south west, anchor=north west] (leg) {
  \tikz\draw[done, line width=1.4pt] (0,0) rectangle (3mm,3mm); done\quad
  \tikz\draw[wip, line width=1.4pt] (0,0) rectangle (3mm,3mm); in progress\quad
  \tikz\draw[plan, line width=1.4pt] (0,0) rectangle (3mm,3mm); planned\quad
  dashed $=$ conditioning / training signal\qquad
  \S\,refs: \texttt{docs/PROJECT.md}\quad updated 2026-09-23};

\end{tikzpicture}
\end{document}
